// Pushing something to a LL is O(1)

// Popping from the LL is O(n)

// Unshifting and shifting to the LL is O(1)

// Mid-insert and Mid-remove is O(n)

// Search by value or index is O(n)

// Summary LL vs Arrays:

// Same BigO:
// Push O(1)
// Insert O(n)
// Delete O(n)
// Lookup by Value O(n)

// Better for Arrays:
// Pop O(1)
// Lookup by Index O(1)


// Better for LL:
// Shift O(1)
// Unshift O(1)

