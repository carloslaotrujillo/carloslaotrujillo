// Linked List don't have indexes

// Linked List don't acuppy continuos space in memory

// The have a variable that points to the "head" item and other called "tail" that points to the last item

// Each item on the list points to the next, and the last one points to "null" (frase "null terminated list")